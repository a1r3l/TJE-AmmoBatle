#include "MenuState.h"

MenuState * MenuState::menuState = NULL;

void MenuState::init(GameManager* gameManager) {

	this->gameManager = gameManager;
}

void MenuState::update(float et) {

}

void MenuState::render(Camera * cam) {

}
